language: cpp

before_install:
    # - sudo add-apt-repository --yes ppa:ubuntu-sdk-team/ppa
    - sudo add-apt-repository --yes ppa:beineri/opt-qt551
    - sudo apt-get update -qq
    # - sudo apt-get install -qq qt5-qmake qtbase5-dev qtdeclarative5-dev qtmultimedia5-dev libqt5webkit5-dev libsqlite3-dev
    # - sudo apt-get install -qq qt5-default qttools5-dev-tools
    - sudo apt-get install -qq qt55declarative qt55multimedia qt55tools

script:
    - qmake --version
    - qmake wanderer/wanderer.pro
    - make
